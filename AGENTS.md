## Code Standards

1. Don't write nested code too much
2. For a long calling chain, you should draw Sequence Diagram in mermaid
3. You should always use English, unless I command you translate
4. Always apply strict mode in typescript and javascript

## Rules you should follow

You should use `zenity --info --text="opencode said done"` when you done. If there is something you cannot handle(like errors), notify me too.
Make sure I can compile and run both frontend and backend
