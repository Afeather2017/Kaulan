/**
 * API configuration for Kaulan frontend
 *
 * In development, the Vite proxy forwards /api to the backend
 * In production (Tauri build), we need to use the full URL
 */

export const API_BASE = 'http://localhost:2080/api'
